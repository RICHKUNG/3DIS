# 實驗執行計畫：修復 mAP=0.0 問題

**基於診斷結果**: DIAGNOSTIC_RESULTS_2025_11_17.md
**根本原因**: SigLIP 特徵不具區分性 (σ=0.007 << 0.05)
**目標**: mAP 0.0 → 0.01-0.03 (短期), 0.03-0.06 (長期)

---

## 📋 實驗概覽

| Phase | 主要改動 | 預期 mAP | 工時 | 優先級 |
|-------|---------|---------|------|--------|
| **Phase 1** | 切換到 OpenCLIP | 0.01-0.02 | 3h | ⭐⭐⭐⭐⭐ |
| **Phase 2** | 優化特徵提取 | 0.015-0.025 | 6h | ⭐⭐⭐⭐ |
| **Phase 3** | 調整查詢格式 | 0.018-0.030 | 3h | ⭐⭐⭐ |
| **Phase 4** | Fine-tune 模型 | 0.030-0.060 | 2週 | ⭐⭐⭐⭐⭐ |

---

## Phase 1: 切換特徵提取器 (OpenCLIP)

### 目標
驗證 SigLIP → OpenCLIP 是否能提升語義區分度

### 實驗設計

#### 實驗 P1-A: OpenCLIP (laion2b)
**模型**: `hf-hub:laion/CLIP-ViT-L-14-laion2B-s32B-b82K`

**配置文件**: `configs/inference/phase1_openclip_laion2b.yaml`
```yaml
experiment:
  name: "phase1_openclip_laion2b"
  dataset: "multiscan"
  scenes: ["scene_00093_01"]  # 單場景快速測試

inference:
  strategy: "hierarchical"

  feature_extractor:
    type: "openclip"
    model: "hf-hub:laion/CLIP-ViT-L-14-laion2B-s32B-b82K"
    device: "cuda"

  retrieval:
    temperature: 0.2
    min_similarity: 0.005
    top_k_per_level: 200
    scale_semantic_score: 300.0  # 保持與 baseline 一致
    apply_softmax: true

  hierarchical:
    coarse_top_k: 200
    object_top_k: 200
    part_top_k: 100
    coarse_threshold: 0.01
    refinement_threshold: 0.01

  pairing:
    containment_threshold: 0.001
    scale_range: [0.0001, 20.0]

  nms:
    iou_threshold: 0.25
    use_soft_nms: false
    keep_top_k: 300

evaluation:
  enabled: true
  gt_path: "data/search3d_gt/multiscan_data_search3d/multiscan_annotations_search3d/ov_part_annotations"
```

**執行命令**:
```bash
# 1. 創建配置文件
cat > configs/inference/phase1_openclip_laion2b.yaml << 'EOF'
[上述 YAML 內容]
EOF

# 2. 運行實驗
PYTHONPATH=src conda run -n 3Dsiglip python scripts/run_full_pipeline.py \
  --config configs/inference/phase1_openclip_laion2b.yaml \
  2>&1 | tee logs/phase1/p1a_openclip_laion2b.log

# 3. 檢查結果
grep -A 10 '"mAP":' outputs/experiments/phase1_openclip_laion2b/scene_00093_01/pipeline_results.json
```

**預期結果**:
- mAP: 0.010-0.020
- Predictions: 100-200 instances
- 如果 mAP > 0.01 → ✅ 繼續 Phase 2
- 如果 mAP < 0.005 → ❌ 嘗試 P1-B

---

#### 實驗 P1-B: CLIP (OpenAI)
**模型**: `openai/clip-vit-large-patch14-336`

**配置文件**: `configs/inference/phase1_clip_openai.yaml`
```yaml
[與 P1-A 相同，僅修改 model]
feature_extractor:
  type: "clip"
  model: "openai/clip-vit-large-patch14-336"
  device: "cuda"
```

**執行命令**:
```bash
# 創建配置
cat > configs/inference/phase1_clip_openai.yaml << 'EOF'
[上述 YAML 內容]
EOF

# 運行
PYTHONPATH=src conda run -n 3Dsiglip python scripts/run_full_pipeline.py \
  --config configs/inference/phase1_clip_openai.yaml \
  2>&1 | tee logs/phase1/p1b_clip_openai.log
```

---

#### 實驗 P1-C: Baseline 對照
**重跑 SigLIP baseline** 確認可重現性

**執行命令**:
```bash
# 使用 test_a0_baseline.yaml
PYTHONPATH=src conda run -n 3Dsiglip python scripts/run_full_pipeline.py \
  --config configs/inference/test_a0_baseline.yaml \
  --scene scene_00093_01 \
  2>&1 | tee logs/phase1/p1c_siglip_baseline.log
```

---

### 決策點 1

**評估標準**:
```python
if map_openclip > 0.01:
    decision = "✅ OpenCLIP 有效，繼續 Phase 2"
elif map_clip > 0.01:
    decision = "✅ CLIP 有效，用 CLIP 進入 Phase 2"
elif map_openclip > map_siglip:
    decision = "⚠️ 有小幅改善，嘗試 Phase 2 優化"
else:
    decision = "❌ 模型切換無效，跳到 Phase 4 (fine-tuning)"
```

**記錄結果**:
```bash
# 創建結果比較表
cat > PHASE1_RESULTS.md << 'EOF'
# Phase 1 結果比較

| 實驗 | 模型 | mAP | AP25 | AP50 | Predictions | 決策 |
|------|------|-----|------|------|-------------|------|
| P1-A | OpenCLIP laion2b | ? | ? | ? | ? | ? |
| P1-B | CLIP OpenAI | ? | ? | ? | ? | ? |
| P1-C | SigLIP baseline | 0.0 | 0.0 | 0.0 | 127 | ❌ |

**最佳模型**: ?
**進入 Phase 2**: Yes/No
EOF
```

---

## Phase 2: 優化特徵提取

**前提**: Phase 1 找到比 SigLIP 更好的模型

### 目標
修復 `feature_extraction.py` 中的特徵聚合問題

### 改動 2A: 加權幀聚合

**文件**: `src/my3dis/inference/feature_extraction.py`

**當前代碼** (Line 314):
```python
# 問題：簡單平均，模糊幀會稀釋品質
aggregated = torch.stack(frame_features).mean(dim=0)
aggregated = aggregated / aggregated.norm(dim=-1, keepdim=True)
```

**修改為**:
```python
def compute_frame_quality(bbox_size, frame_idx, total_frames):
    """
    計算幀質量權重
    - 中間幀 > 首尾幀 (通常更清晰)
    - 大 bbox > 小 bbox (物體更靠近相機)
    """
    # 時間權重：高斯分佈，中間幀權重高
    temporal_weight = np.exp(-((frame_idx - total_frames/2) / (total_frames/4))**2)

    # 尺寸權重：bbox 越大越好
    size_weight = np.sqrt(bbox_size)

    return temporal_weight * size_weight

def aggregate_features_weighted(self, frame_features, frame_info):
    """
    frame_features: List[(feature_tensor, bbox_size, frame_idx)]
    """
    if len(frame_features) == 1:
        return frame_features[0][0]

    # 計算權重
    weights = []
    features = []
    for feat, bbox_size, frame_idx in frame_features:
        quality = compute_frame_quality(bbox_size, frame_idx, len(frame_features))
        weights.append(quality)
        features.append(feat)

    # 正規化權重
    weights = torch.tensor(weights, device=features[0].device)
    weights = torch.softmax(weights, dim=0)  # softmax 確保和為 1

    # 加權聚合
    features_stack = torch.stack(features)  # (N_frames, feat_dim)
    aggregated = (features_stack * weights[:, None]).sum(dim=0)

    # L2 正規化
    aggregated = aggregated / aggregated.norm(dim=-1, keepdim=True)

    return aggregated
```

**修改位置**: `src/my3dis/inference/feature_extraction.py:314`

---

### 改動 2B: 自適應 Bbox Margin

**當前代碼** (Line 288):
```python
# 問題：固定 20px，小物體太大，大物體太小
margin = 20
```

**修改為**:
```python
# 自適應 margin：bbox 的 10%，最小 5px，最大 50px
bbox_width = x2 - x1
bbox_height = y2 - y1
bbox_size = max(bbox_width, bbox_height)

margin = int(0.1 * bbox_size)
margin = max(5, min(margin, 50))  # clamp to [5, 50]
```

---

### 改動 2C: 多幀選擇策略

**當前**: 使用所有幀

**修改為**: 選擇多樣化的幀子集

```python
def select_diverse_frames(all_frames, max_frames=5):
    """
    選擇時間上均勻分佈的幀，避免連續幀冗餘
    """
    if len(all_frames) <= max_frames:
        return all_frames

    # 均勻採樣
    indices = np.linspace(0, len(all_frames)-1, max_frames, dtype=int)
    return [all_frames[i] for i in indices]
```

---

### 實驗 P2-A: 加權聚合

**配置**: 在 Phase 1 最佳模型基礎上，啟用 weighted aggregation

**代碼修改**:
```bash
# 1. 備份原始文件
cp src/my3dis/inference/feature_extraction.py \
   src/my3dis/inference/feature_extraction.py.backup

# 2. 應用改動 2A
# (手動修改或使用 patch)

# 3. 運行實驗
PYTHONPATH=src conda run -n 3Dsiglip python scripts/run_full_pipeline.py \
  --config configs/inference/phase2_weighted_agg.yaml \
  2>&1 | tee logs/phase2/p2a_weighted_agg.log
```

**配置文件**:
```yaml
# configs/inference/phase2_weighted_agg.yaml
# 繼承 Phase 1 最佳配置，添加：
experiment:
  name: "phase2_weighted_agg"

# 其他配置同 Phase 1 最佳模型
```

---

### 實驗 P2-B: 自適應 Margin

**代碼修改**: 應用改動 2B

**執行**:
```bash
PYTHONPATH=src conda run -n 3Dsiglip python scripts/run_full_pipeline.py \
  --config configs/inference/phase2_adaptive_margin.yaml \
  2>&1 | tee logs/phase2/p2b_adaptive_margin.log
```

---

### 實驗 P2-C: 組合優化

**代碼修改**: 同時應用 2A + 2B + 2C

**執行**:
```bash
PYTHONPATH=src conda run -n 3Dsiglip python scripts/run_full_pipeline.py \
  --config configs/inference/phase2_combined.yaml \
  2>&1 | tee logs/phase2/p2c_combined.log
```

---

### 決策點 2

**評估**:
```python
baseline_map = Phase1最佳結果
p2a_map = 加權聚合結果
p2b_map = 自適應margin結果
p2c_map = 組合結果

best_p2 = max(p2a_map, p2b_map, p2c_map)

if best_p2 > baseline_map + 0.005:
    decision = "✅ Phase 2 有效，增益 > 0.005"
else:
    decision = "⚠️ Phase 2 增益小，可能需要 Phase 4"
```

---

## Phase 3: 查詢格式調整

**前提**: Phase 1 + Phase 2 達到 mAP > 0.01

### 目標
測試不同的組合查詢格式，找到模型理解最好的表達方式

### 查詢格式變體

**當前**: `"{part_text} of {object_text}"`

**測試變體**:
```python
query_formats = {
    "format_A": "{part_text} of {object_text}",        # "door of cabinet"
    "format_B": "{object_text} {part_text}",           # "cabinet door"
    "format_C": "{part_text} on {object_text}",        # "door on cabinet"
    "format_D": "{object_text} with {part_text}",      # "cabinet with door"
    "format_E": "a {part_text} that is part of {object_text}",  # verbose
    "format_F": "{part_text} attached to {object_text}",  # "door attached to cabinet"
}
```

### 實驗 P3-A: 格式網格搜索

**代碼修改**: `src/my3dis/inference/strategies/combined_query_mixin.py:77`

```python
def create_combined_query(self, object_text: str, part_text: str, format_type: str = "A") -> str:
    formats = {
        "A": f"{part_text} of {object_text}",
        "B": f"{object_text} {part_text}",
        "C": f"{part_text} on {object_text}",
        "D": f"{object_text} with {part_text}",
        "E": f"a {part_text} that is part of {object_text}",
        "F": f"{part_text} attached to {object_text}",
    }
    return formats.get(format_type, formats["A"])
```

**批次測試腳本**:
```bash
# scripts/test_query_formats.sh
#!/bin/bash

for format in A B C D E F; do
    echo "Testing format: $format"

    # 創建臨時配置
    sed "s/query_format: A/query_format: $format/" \
        configs/inference/phase3_base.yaml > \
        /tmp/phase3_format_${format}.yaml

    # 運行
    PYTHONPATH=src conda run -n 3Dsiglip python scripts/run_full_pipeline.py \
      --config /tmp/phase3_format_${format}.yaml \
      --scene scene_00093_01 \
      2>&1 | tee logs/phase3/p3_format_${format}.log

    # 提取 mAP
    map=$(grep '"mAP":' outputs/experiments/phase3_format_${format}/scene_00093_01/pipeline_results.json | grep -oP '[\d\.]+' | head -1)
    echo "Format $format: mAP = $map" >> PHASE3_RESULTS.txt
done

# 找最佳格式
echo "=== Best Format ===" >> PHASE3_RESULTS.txt
sort -t= -k2 -nr PHASE3_RESULTS.txt | head -1 >> PHASE3_RESULTS.txt
```

**執行**:
```bash
chmod +x scripts/test_query_formats.sh
./scripts/test_query_formats.sh
```

---

### 決策點 3

**評估**:
```python
best_format = 根據 PHASE3_RESULTS.txt
improvement = best_format_map - baseline_map

if improvement > 0.003:
    decision = "✅ 找到更好的查詢格式"
    action = "更新 combined_query_mixin.py 預設格式"
else:
    decision = "⚠️ 查詢格式影響小"
    action = "保持原格式，考慮 Phase 4"
```

---

## Phase 4: Fine-tune 模型 (長期)

**前提**: Phase 1-3 完成，但 mAP < 0.03

### 目標
在 MultiScan 訓練集上 fine-tune 特徵提取器

### 數據準備

#### 步驟 1: 收集訓練樣本
```bash
# 從訓練場景提取 part-object 對
python scripts/prepare_finetune_dataset.py \
  --scenes-dir data/multiscan/train \
  --annotations data/search3d_gt/.../annotations \
  --output data/finetune_pairs.json
```

**數據格式**:
```json
{
  "samples": [
    {
      "image_path": "scene_00001/color/0001200.jpg",
      "bbox": [100, 150, 200, 300],
      "text": "door of cabinet",
      "object_class": "cabinet",
      "part_class": "door",
      "positive": true
    },
    ...
  ]
}
```

#### 步驟 2: 創建對比學習數據集
```python
# Hard negatives:
# - 同 object 不同 part
# - 同 part 不同 object
# - 完全不相關
```

---

### Fine-tune 設定

**模型**: OpenCLIP (Phase 1 最佳)

**訓練腳本**: `scripts/finetune_openclip.py`
```python
import torch
from torch.utils.data import DataLoader
from transformers import CLIPModel, CLIPProcessor
from tqdm import tqdm

# 超參數
config = {
    "model": "hf-hub:laion/CLIP-ViT-L-14-laion2B-s32B-b82K",
    "lr": 1e-5,
    "batch_size": 32,
    "epochs": 10,
    "warmup_steps": 500,
    "temperature": 0.07,
}

# Contrastive loss
def contrastive_loss(image_features, text_features, temperature=0.07):
    # 正規化
    image_features = image_features / image_features.norm(dim=-1, keepdim=True)
    text_features = text_features / text_features.norm(dim=-1, keepdim=True)

    # 相似度矩陣
    logits = (image_features @ text_features.T) / temperature

    # 對角線為正樣本
    labels = torch.arange(len(logits), device=logits.device)

    # 雙向 loss
    loss_i2t = F.cross_entropy(logits, labels)
    loss_t2i = F.cross_entropy(logits.T, labels)

    return (loss_i2t + loss_t2i) / 2

# 訓練循環
for epoch in range(config["epochs"]):
    for batch in tqdm(train_loader):
        images, texts = batch

        # 前向
        image_features = model.get_image_features(images)
        text_features = model.get_text_features(texts)

        # 計算 loss
        loss = contrastive_loss(image_features, text_features, config["temperature"])

        # 反向
        optimizer.zero_grad()
        loss.backward()
        optimizer.step()

    # 驗證
    val_map = evaluate_on_val_set(model, val_scenes)
    print(f"Epoch {epoch}: val_mAP = {val_map:.4f}")
```

**執行**:
```bash
PYTHONPATH=src conda run -n 3Dsiglip python scripts/finetune_openclip.py \
  --config configs/finetune/openclip_multiscan.yaml \
  --train-data data/finetune_pairs.json \
  --val-scenes scene_00093_01,scene_00065_00 \
  --output checkpoints/openclip_finetuned
```

---

### 評估 Fine-tuned 模型

**測試配置**: `configs/inference/phase4_finetuned.yaml`
```yaml
feature_extractor:
  type: "openclip"
  model: "checkpoints/openclip_finetuned/best_model.pt"
  device: "cuda"
```

**執行**:
```bash
PYTHONPATH=src conda run -n 3Dsiglip python scripts/run_full_pipeline.py \
  --config configs/inference/phase4_finetuned.yaml \
  --multi-scene \
  2>&1 | tee logs/phase4/p4_finetuned_multiscene.log
```

**預期結果**:
- mAP: 0.03-0.06
- 如果達到 → ✅ 部署為新 baseline
- 如果未達到 → 檢討數據質量、超參數

---

## 🗓️ 時間表

### Week 1 (12 小時)
- **Day 1-2**: Phase 1 (6h)
  - P1-A: OpenCLIP 測試
  - P1-B: CLIP 測試
  - P1-C: Baseline 對照
  - 決策點 1

- **Day 3-4**: Phase 2 (6h)
  - 實作改動 2A, 2B, 2C
  - 運行 P2-A, P2-B, P2-C
  - 決策點 2

### Week 2 (3 小時)
- **Day 5**: Phase 3 (3h)
  - 格式網格搜索
  - 決策點 3

### Week 3-4 (如需要)
- **Phase 4**: Fine-tuning (40-80h)
  - 數據準備 (8h)
  - 訓練 (24-48h GPU time)
  - 評估 (8h)

---

## 📊 成功指標

### 短期目標 (Phase 1-3)
- ✅ mAP > 0.01 (可接受)
- ✅ mAP > 0.02 (良好)
- ✅ mAP > 0.03 (優秀)

### 長期目標 (Phase 4)
- ✅ mAP > 0.04 (可接受)
- ✅ mAP > 0.05 (良好)
- ✅ mAP > 0.06 (優秀，接近 SOTA)

### 額外指標
- Predictions 數量: 100-200 (合理範圍)
- Oracle mAP 利用率: actual_mAP / oracle_mAP > 0.05
- Feature discriminability: std > 0.10

---

## 📝 實驗記錄

### 記錄模板

**每個實驗創建記錄**:
```bash
cat > logs/phase1/P1A_RECORD.md << 'EOF'
# 實驗 P1-A: OpenCLIP laion2b

**日期**: 2025-11-17
**實驗者**: Claude

## 配置
- 模型: hf-hub:laion/CLIP-ViT-L-14-laion2B-s32B-b82K
- Scene: scene_00093_01
- 其他參數: 同 baseline

## 結果
- mAP: ?
- AP25: ?
- AP50: ?
- Predictions: ?

## 觀察
- [記錄任何異常、警告、特殊現象]

## 決策
- [進入下一步 / 調整 / 放棄]
EOF
```

---

## 🚀 Quick Start

### 立即開始 Phase 1

```bash
# 1. 創建必要目錄
mkdir -p logs/phase1 logs/phase2 logs/phase3 logs/phase4
mkdir -p configs/inference

# 2. 創建 Phase 1 配置
cat > configs/inference/phase1_openclip_laion2b.yaml << 'EOF'
experiment:
  name: "phase1_openclip_laion2b"
  dataset: "multiscan"
  scenes: ["scene_00093_01"]
  levels: [2, 4, 6]
  frames: "1200:1600:20"
  output_root: "outputs/experiments"

inference:
  strategy: "hierarchical"

  feature_extractor:
    type: "openclip"
    model: "hf-hub:laion/CLIP-ViT-L-14-laion2B-s32B-b82K"
    device: "cuda"

  retrieval:
    temperature: 0.2
    min_similarity: 0.005
    top_k_per_level: 200
    scale_semantic_score: 300.0
    apply_softmax: true

  hierarchical:
    coarse_top_k: 200
    object_top_k: 200
    part_top_k: 100
    coarse_threshold: 0.01
    refinement_threshold: 0.01

  pairing:
    containment_threshold: 0.001
    scale_range: [0.0001, 20.0]

  nms:
    iou_threshold: 0.25
    use_soft_nms: false
    keep_top_k: 300

evaluation:
  enabled: true
  gt_path: "data/search3d_gt/multiscan_data_search3d/multiscan_annotations_search3d/ov_part_annotations"

stages:
  ssam:
    enabled: false  # 使用已有 aggregation_output
  filter:
    enabled: false
  tracker:
    enabled: false

  inference:
    enabled: true

  report:
    enabled: true
EOF

# 3. 運行 Phase 1-A
echo "Starting Phase 1-A: OpenCLIP test..."
PYTHONPATH=src conda run -n 3Dsiglip python scripts/run_full_pipeline.py \
  --config configs/inference/phase1_openclip_laion2b.yaml \
  2>&1 | tee logs/phase1/p1a_openclip_laion2b.log

# 4. 查看結果
echo "Extracting results..."
grep -A 10 '"mAP":' outputs/experiments/phase1_openclip_laion2b/scene_00093_01/pipeline_results.json

# 5. 創建結果記錄
cat > PHASE1_RESULTS.md << 'EOF'
# Phase 1 實驗結果

| 實驗 | 模型 | mAP | AP25 | AP50 | Predictions | 時間 |
|------|------|-----|------|------|-------------|------|
| P1-A | OpenCLIP laion2b | ? | ? | ? | ? | ? |

**觀察**:
[記錄結果]

**決策**:
[下一步行動]
EOF

echo "Phase 1-A complete. Review PHASE1_RESULTS.md"
```

---

## 🔄 決策流程圖

```
診斷完成 (mAP=0.0, σ=0.007)
    ↓
[Phase 1: 切換模型]
    ├─ P1-A: OpenCLIP → mAP > 0.01? ✅ → Phase 2
    ├─ P1-B: CLIP → mAP > 0.01? ✅ → Phase 2
    └─ P1-C: SigLIP → mAP = 0.0 ❌
    ↓
[Phase 2: 優化特徵]
    ├─ P2-A: 加權聚合 → +0.002-0.005
    ├─ P2-B: 自適應margin → +0.001-0.003
    └─ P2-C: 組合 → mAP > 0.015? ✅ → Phase 3
    ↓
[Phase 3: 查詢格式]
    └─ 網格搜索 → mAP > 0.02? ✅ → 部署
                           ❌ → Phase 4
    ↓
[Phase 4: Fine-tune]
    └─ 訓練 → mAP > 0.04? ✅ → 部署
                       ❌ → 重新評估策略
```

---

## 📞 問題處理

### 如果 Phase 1 失敗 (mAP < 0.005)
1. 檢查模型是否正確載入
2. 運行特徵區分度測試：
   ```bash
   python scripts/test_siglip_discriminability.py \
     --exp-dir outputs/experiments/phase1_openclip_laion2b/scene_00093_01 \
     --model "hf-hub:laion/CLIP-ViT-L-14-laion2B-s32B-b82K"
   ```
3. 如果 std 仍然 < 0.05 → 直接跳 Phase 4

### 如果 Phase 2 無改善
- 檢查代碼修改是否生效
- 打印中間變量驗證加權計算
- 可能跳過 Phase 3，直接 Phase 4

### 如果 Phase 4 效果不佳
- 檢查訓練數據質量
- 嘗試不同 learning rate / temperature
- 考慮從頭訓練 vs fine-tune

---

**狀態**: 計畫完成，等待執行 Phase 1

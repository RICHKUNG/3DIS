experiment:
  name: test_1012_00005
  dataset_root: /media/public_dataset2/multiscan
  scenes: null
  # scene_start: scene_00005_00  # replace with first scene name (inclusive) to limit the run
  # scene_end: scene_00044_01    # replace with last scene name (inclusive) to limit the run
  # scene_start: scene_00048_00  # replace with first scene name (inclusive) to limit the run
  # scene_end: scene_00077_01    # replace with last scene name (inclusive) to limit the run
  # scene_start: scene_00086_00  # replace with first scene name (inclusive) to limit the run
  # scene_end: scene_00098_02    # replace with last scene name (inclusive) to limit the run

  # scene_start: scene_00005_00  # replace with first scene name (inclusive) to limit the run
  # scene_end: scene_00022_00    # replace with last scene name (inclusive) to limit the run
  # scene_start: scene_00022_01  # replace with first scene name (inclusive) to limit the run
  # scene_end: scene_00048_01    # replace with last scene name (inclusive) to limit the run
  # scene_start: scene_00051_00  # replace with first scene name (inclusive) to limit the run
  # scene_end: scene_00065_01    # replace with last scene name (inclusive) to limit the run
  # scene_start: scene_00075_00  # replace with first scene name (inclusive) to limit the run
  # scene_end: scene_00089_01    # replace with last scene name (inclusive) to limit the run
  # scene_start: scene_00091_00  # replace with first scene name (inclusive) to limit the run
  # scene_end: scene_00098_02    # replace with last scene name (inclusive) to limit the run

  scene_start: scene_00005_00  # replace with first scene name (inclusive) to limit the run
  scene_end: scene_00005_00    # replace with last scene name (inclusive) to limit the run
  parallel_scenes: 1
  levels:
    - 2
    - 4
    - 6
  tag: 246_filter_fill_prop30_claude_test
  aggregate_output: false
  output_layout: scene_level
  output_root: /media/Pluto/richkung/My3DIS/outputs/experiments/{name}
  # run_dir: /media/Pluto/richkung/My3DIS/outputs/experiments/test_1009/scene_00065_00/246_filter_fill_prop30
  frames:
    step: 50
stages:
  gpu: 1  # a
  ssam:
    enabled: true
    ssam_freq: 2
    min_area: 1000
    stability_threshold: 1.0
    persist_raw: true
    skip_filtering: true
    add_gaps: true
    fill_area: 5000
    append_timestamp: false
    downscale_masks: true
    downscale_ratio: 0.3
  filter:
    enabled: true
    min_area: 500
    stability_threshold: 1.0
  tracker:
    enabled: true
    prompt_mode: all_mask
    max_propagate: 30
    iou_threshold: 0.6
    downscale_masks: true
    downscale_ratio: 0.3
    render_viz: true
    comparison_sampling:
      max_frames: 60
  report:
    enabled: true
    name: report.md
    max_width: 640
    record_timings: true
    timing_output: stage_timings.json
